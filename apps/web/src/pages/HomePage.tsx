import EmptyState from '../components/states/EmptyState'

function HomePage() {
  return (
    <EmptyState title="Lobby" message="Authenticated home, no interface yet." />
  )
}

export default HomePage
