# Copy this file to .env (same directory) and adjust the values.

# Port the HTTP server listens on. Optional, defaults to 3000.
PORT=3000

# Postgres connection string. Required — the app refuses to boot without it.
DATABASE_URL=postgresql://transcendence:transcendence@localhost:5432/transcendence
